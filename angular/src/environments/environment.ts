// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  base_url: "https://localhost:7184",
  baseApiUrl: "https://localhost:7184",
  userId: true ? true ? "04cf63f8-928b-4df2-a9fc-01e96f39cd80" : "4064537c-57d0-45e0-963c-d91b07ee32b2" : false ? "67cacc1e-1664-4210-963f-512574cde2ca" : "67cacc1e-1664-4210-963f-512574cde2cb" 
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
